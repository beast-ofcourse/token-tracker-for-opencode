{
  "name": "opencode-token-tracker-cli",
  "version": "0.5.0",
  "description": "Local, read-only dashboard and CLI for OpenCode token usage, spend, and budget",
  "keywords": ["opencode", "tokens", "usage", "dashboard", "cli", "sqlite"],
  "license": "MIT",
  "bin": {
    "tracker": "bin/tracker.js"
  },
  "files": [
    "bin",
    "tracker",
    "assets",
    "config.example.json",
    "requirements.txt",
    "README.md",
    "!tracker/__pycache__"
  ],
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "test": "python -m pytest"
  }
}
